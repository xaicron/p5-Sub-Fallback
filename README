This is Perl module Sub::Fallback.

INSTALLATION

Sub::Fallback installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan Sub::Fallback
or

    % cpanm Sub::Fallback

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

Sub::Fallback documentation is available as in POD. So you can do:

    % perldoc Sub::Fallback

to read the documentation online with your favorite pager.

xaicron
